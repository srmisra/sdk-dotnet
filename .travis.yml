language:
    csharp

install:
    - sudo apt-get install nunit-console

script:
    - xbuild ./Authorize.NET/AuthorizeNET.csproj
    - xbuild ./AuthorizeNETtest/AuthorizeNETtest.csproj
#   make mono happy by copying the config file with project name
    - cp AuthorizeNETtest/App.config AuthorizeNETtest/AuthorizeNETtest.config
    - nunit-console ./AuthorizeNETtest/AuthorizeNETtest.csproj -run=AuthorizeNet.Api.Controllers.MockTest -exclude Integration,NotWorkingOnMono
    - export LD_LIBRARY_PATH=/usr/local/lib
    - mono --debug --profile=monocov:outfile=monocovCoverage.cov,+AuthorizeNET ./src/NUnit-2.6.3/bin/nunit-console.exe --process=Single ./src/AuthorizeNETtest/bin/Debug/AuthorizeNETtest.dll
    - monocov --export-xml=monocovCoverage monocovCoverage.cov
    - cat monocovCoverage.cov
    - ls monocovCoverage
    - REPO_COMMIT_AUTHOR=$(git show -s --pretty=format:"%cn")
    - REPO_COMMIT_AUTHOR_EMAIL=$(git show -s --pretty=format:"%ce")
    - REPO_COMMIT_MESSAGE=$(git show -s --pretty=format:"%s")
    - echo $TRAVIS_COMMIT
    - echo $TRAVIS_BRANCH
    - echo $REPO_COMMIT_AUTHOR
    - echo $REPO_COMMIT_AUTHOR_EMAIL
    - echo $REPO_COMMIT_MESSAGE
    - echo $TRAVIS_JOB_ID
    - mono ./src/packages/coveralls.net.0.6.0/tools/csmacnz.Coveralls.exe --monocov -i ./monocovCoverage --repoToken $COVERALLS_REPO_TOKEN --commitId $TRAVIS_COMMIT --commitBranch $TRAVIS_BRANCH --commitAuthor "$REPO_COMMIT_AUTHOR" --commitEmail "$REPO_COMMIT_AUTHOR_EMAIL" --commitMessage "$REPO_COMMIT_MESSAGE" --jobId $TRAVIS_JOB_ID  --serviceName travis-ci  --useRelativePaths
